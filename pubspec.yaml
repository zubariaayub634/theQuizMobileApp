name: thequestion
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  http: ^0.12.2
  shared_preferences: ^0.5.12+4
  admob_flutter: ^1.0.1
  better_player: ^0.0.13
  chewie_audio: ^1.1.1
  audioplayerui: ^0.0.7
  firebase_auth: ^0.11.1+1
  device_id: ^0.2.0
  google_fonts: ^0.7.0
  cloud_firestore: ^0.13.1
  progress_dialog: ^1.2.1
  fluttertoast: ^3.1.3
  provider: ^4.3.2+2
  rflutter_alert: ^2.0.1
  path_provider: ^1.4.0
  flutter_localizations:
   sdk: flutter  
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/lang/
    - assets/img/
    - myquestions/questions.json